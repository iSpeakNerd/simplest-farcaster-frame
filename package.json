{
  "scripts": {
    "serve": "live-server --port=5050",
    "tunnel": "ngrok http 5050 --log=stdout",
    "dev": "concurrently \"npm run serve\" \"npm run tunnel\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "ngrok": "^5.0.0-beta.2"
  }
}
